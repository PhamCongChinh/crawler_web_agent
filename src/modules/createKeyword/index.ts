// src/modules/createKeyword/index.ts
import logger from "../../config/logger.config.js";

/**
 * Stub module cho createKeyword.
 * Sau này nếu cần logic thật, ta sẽ implement lại.
 */
export const createKeyword = async (): Promise<void> => {
    logger.info("createKeyword module (stub) đang chạy. Chưa implement logic.");
};

export default createKeyword;
